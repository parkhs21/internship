pipeline {
    agent any
    
    stages() {
        stage('git clone') {
            steps() {
                git branch: 'main', credentialId: 'github', url: 'https://github.com/parkhs21/internship'
            }
        }
        
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }     
    }
}