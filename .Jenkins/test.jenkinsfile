pipeline {
    agent any
    
    stages() {
        stage('git clone') {
            steps() {
                git 'https://github.com/parkhs21/internship'
            }
        }
        
        stage('Test') {
            steps {
                sh 'ls -al'
            }
        }   
    }
}