name: python test

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up python 3.9
        uses: actions/setup-python@v2
        with:
          python-version: 3.9
